const int gridSize = 3;
const int puzzleSize = gridSize * gridSize;

namespace config
{
	// Refresh rate (in ms) to show the next move of the puzzle when displaying solution
	const int consoleOutputSpeed = 500;
}